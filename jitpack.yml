before_install:
  - echo "Skipping default build"

install:
  - mvn install:install-file -Dfile=adflowtp-2.2.6.aar -DgroupId=com.adflow.sdk -DartifactId=adflowtp -Dversion=2.2.6 -Dpackaging=aar -DgeneratePom=true -DlocalRepositoryPath=/home/jitpack/.m2/repository
  - ls -l /home/jitpack/.m2/repository/com/adflow/sdk/adflowtp/2.2.6  # 调试